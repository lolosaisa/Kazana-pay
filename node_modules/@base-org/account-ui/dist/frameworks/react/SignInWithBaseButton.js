import { createElement, useEffect, useRef } from 'react';
import { mountSignInWithBaseButton, unmountSignInWithBaseButton, } from '../preact/mountSignInWithBaseButton.js';
export const SignInWithBaseButton = (props) => {
    const ref = useRef(null);
    useEffect(() => {
        if (ref.current) {
            // Clone props to avoid extensibility issues between React and Preact
            const clonedProps = Object.assign({}, props);
            mountSignInWithBaseButton(ref.current, clonedProps);
        }
        return () => {
            if (ref.current) {
                unmountSignInWithBaseButton(ref.current);
            }
        };
    }, [props]);
    return createElement('div', {
        ref,
        style: {
            display: 'block',
            width: '100%',
        },
    });
};
//# sourceMappingURL=SignInWithBaseButton.js.map