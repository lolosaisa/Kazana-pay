import { injectFontStyle } from '@base-org/account/ui-assets';
import { render } from 'preact';
import { BasePayButton } from './BasePayButton.js';
export function mountBasePayButton(container, props) {
    injectFontStyle();
    const element = BasePayButton(props);
    render(element, container);
}
export function unmountBasePayButton(container) {
    render(null, container);
}
//# sourceMappingURL=mountBasePayButton.js.map