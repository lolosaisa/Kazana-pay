import { injectFontStyle } from '@base-org/account/ui-assets';
import { render } from 'preact';
import { SignInWithBaseButton } from './SignInWithBaseButton.js';
export function mountSignInWithBaseButton(container, props) {
    injectFontStyle();
    const element = SignInWithBaseButton(props);
    render(element, container);
}
export function unmountSignInWithBaseButton(container) {
    render(null, container);
}
//# sourceMappingURL=mountSignInWithBaseButton.js.map